function myFunction(x) {
    let temp = 2*x + 3;
    return temp;
}

function add(x, y) {
    let temp = x + y;
    return temp;
}

minus = (x, y) => {
    let temp = x - y;
    return temp;
}

((x, y) => {return x*y;})(1, 3)